= DESCRIPTION:

Cookbook to install Librato Silverline: http://www.librato.com.

= REQUIREMENTS:

 * apt cookbook version >= 1.1.1

= INSTALLATION

= ATTRIBUTES:

node[:silverline][:api_token]

node[:silverline][:email_address]

node[:silverline][:server_id_cmd]

node[:silverline][:template_id]

= USAGE:

Just include the default recipe.
